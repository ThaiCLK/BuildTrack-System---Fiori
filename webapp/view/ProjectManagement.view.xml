<mvc:View controllerName="z.bts.buildtrack551.controller.ProjectManagement"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
    xmlns:fb="sap.ui.comp.filterbar">

    <Page id="pageProjectList" showNavButton="true" navButtonPress=".onNavBack"
          showHeader="false">
        <content>
            <VBox width="100%" class="sapUiLargeMarginBottom">
                <fb:FilterBar id="projectFilterBar"
                    useToolbar="false"
                    showGoOnFB="true"
                    search="onFilterSearch"
                    clear="onFilterClear"
                    showClearOnFB="true">
                    <fb:filterGroupItems>
                        <fb:FilterGroupItem name="ProjectCode" label="{i18n>projectCode}" groupName="Basic" visibleInFilterBar="true">
                            <fb:control>
                                <Input id="fbProjectCode"
                                    placeholder="{i18n>selectProjectCode}"
                                    showValueHelp="true"
                                    valueHelpRequest="onValueHelpProjectCodeRequest"/>
                            </fb:control>
                        </fb:FilterGroupItem>
                        <fb:FilterGroupItem name="ProjectName" label="{i18n>projectName}" groupName="Basic" visibleInFilterBar="true">
                            <fb:control>
                                <Input id="fbProjectName"
                                    placeholder="{i18n>selectProjectName}"
                                    showValueHelp="true"
                                    valueHelpRequest="onValueHelpProjectNameRequest"/>
                            </fb:control>
                        </fb:FilterGroupItem>
                        <fb:FilterGroupItem name="Status" label="{i18n>status}" groupName="Basic" visibleInFilterBar="true">
                            <fb:control>
                                <Input id="fbStatus"
                                    placeholder="{i18n>selectStatus}"
                                    showValueHelp="true"
                                    valueHelpRequest="onValueHelpStatusRequest"/>
                            </fb:control>
                        </fb:FilterGroupItem>
                        <fb:FilterGroupItem name="ProjectType" label="{i18n>projectType}" groupName="Basic" visibleInFilterBar="true">
                            <fb:control>
                                <Input id="fbType"
                                    placeholder="{i18n>selectType}"
                                    showValueHelp="true"
                                    valueHelpRequest="onValueHelpTypeRequest"/>
                            </fb:control>
                        </fb:FilterGroupItem>
                        <fb:FilterGroupItem name="StartDate" label="{i18n>startDate}" groupName="Basic" visibleInFilterBar="true">
                            <fb:control>
                                <DatePicker id="fbStartDate" displayFormat="dd/MM/yyyy" valueFormat="yyyy-MM-dd"/>
                            </fb:control>
                        </fb:FilterGroupItem>
                        <fb:FilterGroupItem name="EndDate" label="{i18n>endDate}" groupName="Basic" visibleInFilterBar="true">
                            <fb:control>
                                <DatePicker id="fbEndDate" displayFormat="dd/MM/yyyy" valueFormat="yyyy-MM-dd"/>
                            </fb:control>
                        </fb:FilterGroupItem>
                    </fb:filterGroupItems>
                </fb:FilterBar>

                <Table id="projectTable"
                    inset="false"
                    alternateRowColors="true"
                    mode="MultiSelect"
                    fixedLayout="false"
                    selectionChange=".onProjectSelectionChange"
                    items="{ path: 'pm>/paginatedProjects' }">

                <headerToolbar>
                    <OverflowToolbar id="otbSubheader" style="Clear">
                        <!-- Back chevron + title -->
                        <Button icon="sap-icon://nav-back" type="Transparent" press=".onNavBack"
                                tooltip="{i18n>backToDashboard}"/>
                        <Title id="tableHeader" text="{i18n>projectList}" level="H2"/>
                        <ToolbarSpacer id="toolbarSpacer"/>
                        <Button
                            id="btnCreateProject"
                            text="{i18n>createProject}"
                            type="Emphasized"
                            icon="sap-icon://add"
                            press=".onPressCreate"/>
                        <Button
                            id="btnEditProject"
                            text="{i18n>edit}"
                            icon="sap-icon://edit"
                            type="Transparent"
                            enabled="{= ${viewState>/selectedProjectCount} > 0 }"
                            press=".onEditProject"/>
                        <Button
                            id="btnDeleteProject"
                            text="{i18n>delete}"
                            icon="sap-icon://delete"
                            type="Transparent"
                            enabled="{= ${viewState>/selectedProjectCount} > 0 }"
                            press=".onDeleteProject"/>
                        <Button
                            id="btnCloseProject"
                            text="{i18n>closeProject}"
                            icon="sap-icon://decline"
                            type="Transparent"
                            enabled="{viewState>/canCloseProject}"
                            press=".onCloseProject"/>
                    </OverflowToolbar>
                </headerToolbar>

                <infoToolbar>
                    <OverflowToolbar style="Clear">
                        <ToolbarSpacer/>
                        <Button icon="sap-icon://navigation-left-arrow" type="Transparent" press=".onPrevPage" enabled="{pm>/canPrev}" />
                        <Text text="{pm>/currentPage} / {pm>/totalPages}" />
                        <Button icon="sap-icon://navigation-right-arrow" type="Transparent" press=".onNextPage" enabled="{pm>/canNext}" />
                        <ToolbarSpacer/>
                    </OverflowToolbar>
                </infoToolbar>

                <columns>
                    <Column id="colProjectCode">
                        <Text id="txtColProjectCode" text="{i18n>projectCode}"/>
                    </Column>
                    <Column id="colProjectName">
                        <Text id="txtColProjectName" text="{i18n>projectName}"/>
                    </Column>
                    <Column id="colProjectType" hAlign="Center">
                        <Text id="txtColProjectType" text="{i18n>type}"/>
                    </Column>
                    <Column id="colCreatedBy" minScreenWidth="Desktop" demandPopin="true">
                        <Text id="txtColCreatedBy" text="{i18n>createBy}"/>
                    </Column>
                    <Column id="colStartDate" hAlign="End">
                        <Text id="txtColStartDate" text="{i18n>startDate}"/>
                    </Column>
                    <Column id="colEndDate" hAlign="End">
                        <Text id="txtColEndDate" text="{i18n>estEndDate}"/>
                    </Column>
                    <Column id="colStatus" hAlign="Center" width="10rem">
                        <Text id="txtColStatus" text="{i18n>status}"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem id="cliProjectTemplate" vAlign="Middle"
                                    type="Navigation" press=".onPressProject">
                        <cells>
                            <!-- Project Code: bold identifier -->
                            <ObjectIdentifier id="objProjectCode"
                                title="{pm>ProjectCode}"
                                titleActive="false"/>

                            <!-- Project Name -->
                            <ObjectIdentifier id="objProjectName"
                                title="{pm>ProjectName}"
                                titleActive="false"/>

                            <!-- Type badge — icon & colour via formatter (avoids XML whitespace issues) -->
                            <ObjectStatus id="objProjectType"
                                text="{ path: 'pm>ProjectType', formatter: '.formatTypeText' }"
                                icon="{ path: 'pm>ProjectType', formatter: '.formatTypeIcon' }"
                                state="{ path: 'pm>ProjectType', formatter: '.formatTypeState' }"
                                inverted="true"/>

                            <!-- Created By -->
                            <Text id="txtCreatedBy" text="{pm>CreatedByName}"/>

                            <!-- Start Date -->
                            <Text id="txtStartDate" text="{
                                path: 'pm>StartDate',
                                type: 'sap.ui.model.type.Date',
                                formatOptions: { pattern: 'dd/MM/yyyy' }
                            }"/>

                            <!-- End Date — highlight overdue in red -->
                            <ObjectStatus id="objEndDate"
                                text="{
                                    path: 'pm>EndDate',
                                    type: 'sap.ui.model.type.Date',
                                    formatOptions: { pattern: 'dd/MM/yyyy' }
                                }"
                                state="None"/>

                            <!-- Status badge with semantic colours -->
                            <ObjectStatus id="objStatus"
                                class="statusBadgeNoWrap"
                                text="{path: 'pm>Status', formatter: '.formatStatusText'}"
                                state="{= ${pm>Status} === 'IN_PROGRESS' ? 'Warning' :
                                         ${pm>Status} === 'PLANNING' ? 'Information' :
                                         ${pm>Status} === 'CLOSED' ? 'Success' :
                                         'None' }"
                                inverted="true"/>
                        </cells>
                    </ColumnListItem>
                </items>

                <noData>
                    <IllustratedMessage
                        illustrationType="sapIllus-NoData"
                        title="{i18n>noProjectsFound}"
                        description="{i18n>noProjectsFoundDesc}"/>
                </noData>

                </Table>
            </VBox>
        </content>
    </Page>
</mvc:View>