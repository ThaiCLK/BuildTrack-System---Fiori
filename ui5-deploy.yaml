# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: z.bts.buildtrack551
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://s35.gb.ucc.cit.tum.de
          client: '302'
        app:
          name: ZBT_BUILDTRACK
          description: Build Track Fiori App
          package: Z_SAP490_G1_SP26
          transport: S35K902230
        exclude:
          - /test/
